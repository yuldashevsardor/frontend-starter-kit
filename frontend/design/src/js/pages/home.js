$(() => {
    const app = window.app;
    const components = app.components;
    const mediaComponent = components.media;
    const pageSelector = 'body#home-page';
    const page = $(pageSelector);

    if (!page.length) {
        return false;
    }
});



